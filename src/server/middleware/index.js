const allowCrossDomain = require('./allowCrossDomain');
const upload = require('./upload');
const generateId = require('./generateId');
const simulateFetchTime = require('./simulateFetchTime');

module.exports = {
  allowCrossDomain,
  upload,
  generateId,
  simulateFetchTime
};
